export enum Roles {
  ADMIN = "admin",
  PATIENT = "patient",
  NORMAL = "normal",
}

export enum EventStatus {
  PENDING = "pending",
  HELD = "held",
  UPCOMING = "upcoming",
}

export enum RequestType {
  PATIENT = "patient",
  CHILD = "child",
  ELDER = "elder",
}

export enum RequestStatus {
  PENDING = "pending",
  ONGOING = "ongoing",
  COMPLETED = "completed",
}

export enum DonationType {
  ONE_TIME = "one-time",
  MONTHLY = "monthly",
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other",
}
